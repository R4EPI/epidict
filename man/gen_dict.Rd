% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_dict.R
\name{gen_dict}
\alias{gen_dict}
\title{Generate a data dictionary from a data frame}
\usage{
gen_dict(
  x,
  format = "odk",
  path = NULL,
  constraints = TRUE,
  max_choices = 20,
  id_cols = NULL,
  clean = FALSE,
  ...
)
}
\arguments{
\item{x}{A data frame.}

\item{format}{Character; target format for writing. Currently only
\code{"odk"} is implemented; ignored when \code{path = NULL}.}

\item{path}{Optional file path ending in \code{.xlsx}. If provided the
dictionary is written via \code{\link[=write_dict]{write_dict()}} and returned invisibly. If
\code{NULL} (default) the epidict tibble is returned directly.}

\item{constraints}{Logical; if \code{TRUE} (default) adds constraint
expressions for numeric variables (min/max from the data) and
relevant conditions for pregnancy-related variables (shown only when
sex is female). Constraint messages are in English.}

\item{max_choices}{Integer; maximum number of unique non-NA values before a
character or integer column is treated as free text rather than
categorical. Default \code{20}.}

\item{id_cols}{Character vector of column names to force to type \code{text}
regardless of content (e.g. unique identifiers).}

\item{clean}{Logical; if \code{TRUE}, variable names and choice values are
converted to lower snake case via \code{\link[=tidy_labels]{tidy_labels()}}. Defaults to
\code{FALSE} so that names are preserved as-is.}

\item{...}{Additional arguments passed to the format writer (e.g.
\code{form_title}, \code{form_id} for ODK). See \code{\link[=to_odk]{to_odk()}}.}
}
\value{
A tibble in epidict format (same structure as \code{\link[=read_dict]{read_dict()}} with
\code{compact = TRUE}): one row per variable with columns \code{name},
\code{type}, \code{label}, \code{value_type}, \code{hint},
\code{required}, \code{relevant}, \code{constraint},
\code{constraint_message}, and a nested \code{options} column. Returned
invisibly when \code{path} is given.
}
\description{
Creates a data dictionary from an existing data frame in the same format
as \code{\link[=read_dict]{read_dict()}} and \code{\link[=msf_dict]{msf_dict()}}, so the result can be passed directly
to \code{\link[=gen_data]{gen_data()}} to produce fake data, or used with \pkg{matchmaker} for
recoding. Variable labels and value labels are read from \pkg{labelled}
attributes if present (e.g. data imported via \pkg{haven} from Stata/SPSS).
}
\details{
The primary workflow this enables is:
\enumerate{
\item User runs \code{gen_dict(their_linelist)} and shares the resulting
dictionary instead of their raw data.
\item Recipient calls \code{\link[=gen_data]{gen_data()}} (or the \code{dict} argument directly)
to generate a realistic fake dataset for code development.
}
}
